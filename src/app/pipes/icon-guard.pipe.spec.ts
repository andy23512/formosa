import { IconGuardPipe } from './icon-guard.pipe';

describe('IconGuardPipe', () => {
  it('create an instance', () => {
    const pipe = new IconGuardPipe();
    expect(pipe).toBeTruthy();
  });
});
